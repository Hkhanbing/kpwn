usage: kpwn [-h] {init,create} ...

Generate a kernel pwn struct.

positional arguments:
  {init,create}  Subcommands
    init         kpwn init
    create       kpwn create <filename>

options:
  -h, --help     show this help message and exit
